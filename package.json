{
  "name": "agentfactory",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/engine"
  ],
  "description": "Workspace for building and testing pi agents.",
  "scripts": {
    "sandcastle": "npx tsx .sandcastle/main.ts",
    "pi": "bash -c 'set -a && source models.env && set +a && cd pi-sandbox && exec pi --no-context-files --provider openrouter \"$@\"' --",
    "agent": "bash -c 'set -a && source models.env && set +a && exec node scripts/run-agent.mjs \"$@\"' --",
    "mesh": "bash -c 'set -a && source models.env && set +a && exec node scripts/launch-mesh.mjs \"$@\"' --",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.75.4",
    "@earendil-works/pi-coding-agent": "^0.75.4",
    "@earendil-works/pi-tui": "^0.75.4",
    "@xterm/headless": "^6.0.0",
    "node-pty": "^1.1.0",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@ai-hero/sandcastle": "^0.5.6",
    "vitest": "^4.1.5"
  }
}
