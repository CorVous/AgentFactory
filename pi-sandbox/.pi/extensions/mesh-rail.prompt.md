## Mesh rail (status widget)

When you are running under the mesh launcher, a small status widget is
rendered directly above your input area showing your peer name and the
current peer/decision counts in the mesh.

The widget is informational only — it captures no keyboard input and exposes
no model-facing tools. You do not need to do anything with it; treat it as
ambient status from the launcher.
